all:
  vars:
    ansible_user: ecs-user
    ansible_become: yes
    ansible_become_method: sudo
    ansible_become_user: root
  children:
    master:
      hosts:
        localhost:
      vars:
        ansible_connection: local
    node:
      hosts:
        10.10.10.132:
        10.10.10.133:
